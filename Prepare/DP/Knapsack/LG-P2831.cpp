#include <bits/stdc++.h>

using namespace std;


const int N = 19;
double x[N], y[N];

set<int> st;
int nPol;
int targ[N * N];

inline void needle(int n)
{
	
}

int f[1 << (N + 1) - 1];

inline double solve(int n)
{
	for (int i = 0; i < nP; i++)
	{
		f[]
	}
} 

int main()
{
	int t = 0;
	
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	cin >> t;
	for (int i = 0; i < t; i++)
	{
		int n = 0, m = 0;
		
		cin >> n >> m;
		for (int j = 0; j < n; j++)
		{
			cin >> x[i] >> y[i];
		}
		needle(n);
		
		cout << solve(n) << endl;
	}
	
	return 0;
}
