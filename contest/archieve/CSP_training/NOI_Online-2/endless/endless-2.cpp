#include <bits/stdc++.h>
using namespace std;

int n, l, v, qn; // number, length, velocity, question number
int mag[200010]; // magic
int que[200010]; // question

/*
    Years that Sisyphus need to go.
*/
int year(int tme) // time
{
    for(int i = 0; i <= n; i++)
    {
        
    }
}

int main()
{
    scanf("%d%d%d", &n, &l, &v);
    for (int i = 0; i <n; ++i)
    {
        scanf("%d", mag[i]);
    }
    sort(mag, mag + n);
    scanf("%d", &qn);
    for (int i = 0; i < qn; i++)
    {
        scanf("%d", que[i]);
    }



    return 0;
}