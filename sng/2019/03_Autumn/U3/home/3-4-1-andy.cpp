#include <bits/stdc++.h>
using namespace std; 

int getR(string str, int m)
{
    int r = 0, n = 0, t = 0; 
    for (i = 0 ; i < str.size(); i++)
    {
        t = str[str.size() -i -1] - '0'; 
        r = n + t%m;

    }
    return 0; 
}

int main() 
{
    string s ; 
    cin >> s; 
    cout << s; 

    int n , i ;

    
    
    return 0; 
}